apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: rhoai-platform
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: rhoai-demos
spec:
  description: RHOAI platform components (operators, dependencies, DataScienceCluster)
  
  # Allow applications to deploy to any namespace
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  
  # Allow pulling from these Git repositories
  sourceRepos:
    - https://github.com/dandawg/rhoai-deploy.git
    - https://github.com/dandawg/rhoai-anythingllm-demos.git
  
  # Allow all resource types (platform needs cluster-wide resources)
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  
  # Namespace resource whitelist
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
